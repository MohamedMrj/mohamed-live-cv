Put your project thumbnails here.
Recommended size: 900x520 px.
Supported formats: .png, .jpg, .webp, .svg.

Then update the image field in data.js, for example:
image: "assets/projects/my-project.png"

If image is empty, the website shows a clean fallback automatically.
